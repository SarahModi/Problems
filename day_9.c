// VALID STATEMENTS
#include <stdio.h>

int main(){
  int a = 22
  int b = a
  int c = b * 10;
  int d = 1, e;
return 0;
}

// the compiler reads line by line, so never use a variable before declaring it
// you cannot use and declare a variable at the same time
