//Write a program to calculate area of a square(side is given)
# include <stdio.h>

int main(){
  int side;
  printf("enter  side: ");
    scanf("%f", &side);
    
  printf("area is: %f", side * side);

  return 0;
}
