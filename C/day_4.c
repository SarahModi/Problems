#include <stdio.h>

int main (){
  int age = 27;
  float total_price = 204.5;
  char hashtag = '#';
  printf("age is %d \n", age);
  printf("loss is %f \n", total_price);
  printf("symbol is %c \n", hashtag);
  return 0;
}
